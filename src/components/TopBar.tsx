export default function TopBar() {
  return (
    <header className="top-bar">
      <img src="/logo.png?v=colored" alt="" className="top-bar-logo" />
      <span className="top-bar-title">LCEA Calculator</span>
    </header>
  );
}
